default_install_hook_types: [pre-commit, pre-push]

repos:
  # These hooks are built into prek and run entirely in Rust without cloning
  # pre-commit-hooks or creating a Python environment.
  - repo: builtin
    hooks:
      - id: check-yaml
      - id: check-toml
      - id: check-merge-conflict
      - id: end-of-file-fixer
      - id: trailing-whitespace

  - repo: local
    hooks:
      - id: rust-test
        name: Rust backend tests
        entry: cargo test --manifest-path src-tauri/Cargo.toml
        language: system
        files: ^src-tauri/
        pass_filenames: false

      - id: rust-fmt
        name: Rust formatting
        entry: cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check
        language: system
        files: ^src-tauri/.*\.rs$
        pass_filenames: false

      - id: next-lint
        name: next-lint
        entry: bash -c 'cd weft-frontend && npm run lint'
        language: system
        types_or: [javascript, ts, tsx, jsx]
        pass_filenames: false

      - id: prettier
        name: prettier
        entry: bash -c 'cd weft-frontend && npm run format'
        language: system
        types_or: [javascript, ts, tsx, jsx, json, css]
        pass_filenames: false

      - id: next-build
        name: next-build
        entry: bash -c 'cd weft-frontend && npm run build'
        language: system
        types_or: [javascript, ts, tsx, jsx]
        pass_filenames: false

      - id: backend-test
        name: Rust backend tests
        entry: cargo test --manifest-path src-tauri/Cargo.toml
        language: system
        files: ^src-tauri/
        pass_filenames: false
        stages: [pre-push]

      - id: frontend-coverage
        name: frontend Vitest coverage
        entry: yarn --cwd weft-frontend test:coverage
        language: system
        files: ^weft-frontend/
        pass_filenames: false
        stages: [pre-push]

      - id: frontend-e2e
        name: frontend Playwright
        entry: yarn --cwd weft-frontend test:e2e
        language: system
        files: ^weft-frontend/
        pass_filenames: false
        stages: [pre-push]
