[project]
name = "weft-docs"
version = "0.0.2"
description = "WEFT documentation tooling"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mkdocs-terminal>=4.8.0",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple"
default = true

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]
