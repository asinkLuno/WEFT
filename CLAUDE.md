@AGENTS.md

# Frontend

Package management uses **yarn** (Classic, v1). Do not use npm or pnpm.
All frontend source lives under `src/` — see `src/App.tsx` for the entry component, `src/app/` for route pages, `src/components/` for UI components, and `src/lib/` for utilities.
